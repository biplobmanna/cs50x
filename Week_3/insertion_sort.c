#include <stdio.h>

void insertion_sort(int len, int* arr);

int main(void) {
    int arr[] = {4, 8, 1, 2, 3, 5, 7, 6, 0, 9};
    
    insertion_sort(10, arr);

    return 0;
}

void insertion_sort(int len, int* arr) {
    // find the lowest
}